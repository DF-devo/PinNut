| ID        | Заголовок                             | Severity    | Файл(ы)                                         | Как воспроизвести                   | Ожидаемый результат         | Фактический результат                                        | ✅ Как исправить                                                                  | Статус  |
| --------- | ------------------------------------- | ----------- | ----------------------------------------------- | ----------------------------------- | --------------------------- | ------------------------------------------------------------ | -------------------------------------------------------------------------------- | ------- |
| `BUG-001` | Белый экран при запуске               | 🔴 Critical | `App.tsx`, `FilterBar.tsx`, `NoteForm.tsx`      | `npm run dev` → открыть localhost   | Рендер интерфейса           | Пустой экран, ошибка `does not provide an export named...`   | Заменить `export default` на `export const` в компонентах                        | ✅ Fixed |
| `BUG-002` | Unused imports (TS6133)               | 🟡 Major    | `App.tsx`                                       | Запуск `npm run dev`                | Сборка без ошибок TS        | `error TS6133: 'useState' is declared but never read`        | Удалить неиспользуемые импорты (`useState`, логотипы, `App.css`)                 | ✅ Fixed |
| `BUG-003` | Ошибка импорта стора                  | 🔴 Critical | `App.tsx`                                       | Открыть приложение                  | Корректный импорт стора     | `Failed to resolve import "./store/useNotesStore"`           | Исправить `useNotesStore` → `useNoteStore`                                       | ✅ Fixed |
| `BUG-004` | Артефакты генерации AI                | 🔴 Critical | `NoteForm.tsx`, `NoteList.tsx`, `FilterBar.tsx` | Открыть сгенерированные файлы       | Валидный TSX-синтаксис      | `() = >`, пробелы в `type= "text "`, `& &` вместо `&&`       | Заменить `() = >` → `() =>`, убрать лишние пробелы, исправить `&&`               | ✅ Fixed |
| `BUG-005` | Рассинхрон пропсов формы              | 🟡 Major    | `App.tsx`, `NoteForm.tsx`                       | Заполнить форму → нажать "Добавить" | Заметка сохраняется         | `props.onSubmit is not a function`                           | В `App.tsx`: `<NoteForm onAdd={addNote} />` вместо `onSubmit`                    | ✅ Fixed |
| `BUG-006` | `getTimeIndicator` без `now`          | 🟡 Major    | `NoteList.tsx`                                  | Открыть список с дедлайном          | Цвет времени рассчитывается | `undefined`/`NaN` в цвете, падение рендера                   | Передавать `Date.now()`: `getTimeIndicator(note.deadline, Date.now())`           | ✅ Fixed |
| `BUG-007` | `FilterBar` не получает состояние     | 🟠 Major    | `App.tsx`, `FilterBar.tsx`                      | Клик по тегу в фильтре              | Список фильтруется          | Кнопки кликаются, фильтрация не работает                     | Поднять `activeTag` в `App.tsx`, прокинуть в `FilterBar` и `NoteList`            | ✅ Fixed |
| `BUG-008` | Пути `/PinNut/` в `index.html`        | 🔴 Critical | `index.html`, `vite.config.ts`                  | Запуск `npm run dev`                | Загрузка скриптов Vite      | 404 на `@vite/client`, белый экран                           | Убрать `/PinNut/` из путей, удалить ручные `<script>` теги                       | ✅ Fixed |
| `BUG-009` | Дубликат `<!doctype`                  | 🔴 Critical | `index.html`                                    | Открыть файл в редакторе            | Валидный HTML5              | `<!doctype h<!doctype html>`, парсер Vite падает             | Заменить содержимое на чистый шаблон `<!doctype html>`                           | ✅ Fixed |
| `BUG-010` | Отсутствие CSS-переменных индикаторов | 🟠 Minor    | `index.css`                                     | Рендер `NoteList`                   | Цвета времени применяются   | Прозрачные/серые цвета, `var(--indicator-...)` не определены | Добавить в `:root`: `--indicator-gray`, `--indicator-red`, `--yellow`, `--green` | ✅ Fixed |
