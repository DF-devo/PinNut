| ID        | Заголовок                             | Severity    | Файл(ы)                                         | Как воспроизвести                   | Ожидаемый результат         | Фактический результат                                        | ✅ Как исправить                                                                  | Статус  |
| --------- | ------------------------------------- | ----------- | ----------------------------------------------- | ----------------------------------- | --------------------------- | ------------------------------------------------------------ | -------------------------------------------------------------------------------- | ------- |
| `BUG-001` | Белый экран при запуске               | 🔴 Critical | `App.tsx`, `FilterBar.tsx`, `NoteForm.tsx`      | `npm run dev` → открыть localhost   | Рендер интерфейса           | Пустой экран, ошибка `does not provide an export named...`   | Заменить `export default` на `export const` в компонентах                        | ✅ Fixed |
| `BUG-002` | Unused imports (TS6133)               | 🟡 Major    | `App.tsx`                                       | Запуск `npm run dev`                | Сборка без ошибок TS        | `error TS6133: 'useState' is declared but never read`        | Удалить неиспользуемые импорты (`useState`, логотипы, `App.css`)                 | ✅ Fixed |
| `BUG-003` | Ошибка импорта стора                  | 🔴 Critical | `App.tsx`                                       | Открыть приложение                  | Корректный импорт стора     | `Failed to resolve import "./store/useNotesStore"`           | Исправить `useNotesStore` → `useNoteStore`                                       | ✅ Fixed |
| `BUG-004` | Артефакты генерации AI                | 🔴 Critical | `NoteForm.tsx`, `NoteList.tsx`, `FilterBar.tsx` | Открыть сгенерированные файлы       | Валидный TSX-синтаксис      | `() = >`, пробелы в `type= "text "`, `& &` вместо `&&`       | Заменить `() = >` → `() =>`, убрать лишние пробелы, исправить `&&`               | ✅ Fixed |
| `BUG-005` | Рассинхрон пропсов формы              | 🟡 Major    | `App.tsx`, `NoteForm.tsx`                       | Заполнить форму → нажать "Добавить" | Заметка сохраняется         | `props.onSubmit is not a function`                           | В `App.tsx`: `<NoteForm onAdd={addNote} />` вместо `onSubmit`                    | ✅ Fixed |
| `BUG-006` | `getTimeIndicator` без `now`          | 🟡 Major    | `NoteList.tsx`                                  | Открыть список с дедлайном          | Цвет времени рассчитывается | `undefined`/`NaN` в цвете, падение рендера                   | Передавать `Date.now()`: `getTimeIndicator(note.deadline, Date.now())`           | ✅ Fixed |
| `BUG-007` | `FilterBar` не получает состояние     | 🟠 Major    | `App.tsx`, `FilterBar.tsx`                      | Клик по тегу в фильтре              | Список фильтруется          | Кнопки кликаются, фильтрация не работает                     | Поднять `activeTag` в `App.tsx`, прокинуть в `FilterBar` и `NoteList`            | ✅ Fixed |
| `BUG-008` | Пути `/PinNut/` в `index.html`        | 🔴 Critical | `index.html`, `vite.config.ts`                  | Запуск `npm run dev`                | Загрузка скриптов Vite      | 404 на `@vite/client`, белый экран                           | Убрать `/PinNut/` из путей, удалить ручные `<script>` теги                       | ✅ Fixed |
| `BUG-009` | Дубликат `<!doctype`                  | 🔴 Critical | `index.html`                                    | Открыть файл в редакторе            | Валидный HTML5              | `<!doctype h<!doctype html>`, парсер Vite падает             | Заменить содержимое на чистый шаблон `<!doctype html>`                           | ✅ Fixed |
| `BUG-010` | Отсутствие CSS-переменных индикаторов | 🟠 Minor    | `index.css`                                     | Рендер `NoteList`                   | Цвета времени применяются   | Прозрачные/серые цвета, `var(--indicator-...)` не определены | Добавить в `:root`: `--indicator-gray`, `--indicator-red`, `--yellow`, `--green` | ✅ Fixed |
| `BUG-011` | Краш `toggleItem` на старых заметках  | 🔴 Critical | `useNoteStore.ts`                               | Кликнуть на пункт списка у старой заметки (без поля `items`) | Галочка переключается | `TypeError: Cannot read properties of undefined (reading 'map')`, белый экран | Заменить `n.items.map` → `(n.items ?? []).map` | ✅ Fixed |
| `BUG-012` | Фильтр по тегу не сбрасывается после удаления | 🟡 Major | `App.tsx`                              | Фильтровать по тегу → удалить последнюю заметку с этим тегом | Фильтр сбрасывается, остальные заметки видны | Экран "Нет заметок", сбросить фильтр невозможно | В `handleDelete` проверять, остались ли заметки с `activeTag`, если нет — `setActiveTag(null)` | ✅ Fixed |
| `BUG-013` | NaN в сортировке по приоритету        | 🟡 Major    | `sortNotes.ts`                                  | Сортировка "По приоритету" при наличии старых заметок без поля `priority` | Стабильный порядок | Случайный/нестабильный порядок заметок | `priorityOrder[a.priority] ?? 3` — fallback для невалидного приоритета | ✅ Fixed |
| `BUG-014` | NaN в `useMemo` без дедлайна          | 🟢 Minor    | `NoteCard.tsx`                                  | Открыть заметку без дедлайна        | Мемо не вычисляются | `getTimeIndicator`/`getProgressColor`/`formatDeadline` получают `deadline=""` → возвращают `NaN`, `hsl(NaN)` | Обернуть мемо: `hasDeadline ? getTimeIndicator(...) : undefined` | ✅ Fixed |
| `BUG-015` | GIF корги не загружается              | 🟡 Major    | `OfflineOverlay.tsx`                            | Отключить интернет → открыть оверлей | Анимированный корги | Сломанная картинка (broken image icon) | Переименовать файл без пробелов: `corgi.gif`, обновить импорт | ✅ Fixed |
| `BUG-016` | Пункты чеклиста прыгали после клика   | 🟡 Major    | `NoteCard.tsx`                                  | Кликнуть на пункт списка            | Пункт остаётся на месте | Пункты хаотично перемешивались при каждом рендере | Убрать `sortItems` из рендера, сортировать один раз в store при `toggleItem` + FLIP-анимация | ✅ Fixed |
| `BUG-017` | Краш при копировании заметки-списка   | 🔴 Critical | `NoteForm.tsx`                                  | Копировать заметку типа "список"    | Поля заполняются из заметки | `TypeError` — `note.items` undefined у старых заметок | `setItems(note.items ?? [])` в `handleCopy` | ✅ Fixed |
| `BUG-018` | `getProgressColor` — дедлайн == дата создания | 🟢 Minor | `getProgressColor.ts`                    | Создать заметку с дедлайном "прямо сейчас" | Нейтральный цвет | `total = 0`, `elapsed/0 = Infinity` → прогресс-бар сразу красный | `if (total <= 0) return 'hsl(0, 80%, 50%)'` | 🔴 Open |
| `BUG-019` | `getProgressColor` — дедлайн раньше создания | 🟢 Minor | `getProgressColor.ts`                    | Заметка с дедлайном до `createdAt`  | Красный прогресс (просрочено) | `Math.max(negative, 0) = 0` → зелёный прогресс навсегда | Тот же фикс: `if (total <= 0) return 'hsl(0, 80%, 50%)'` | 🔴 Open |
| `BUG-020` | `formatDeadline` показывает "Через 0 сек" | 🟢 Minor | `formatDeadline.ts`                        | Дедлайн через < 1 секунды           | "Истекает..." или аналог | `seconds = 0` → "Через 0 сек" | `if (seconds < 1) return 'Истекает...'` | 🔴 Open |
| `BUG-021` | Двойной вызов `onClose` в модалке     | 🟢 Minor    | `Modal.tsx`                                     | Быстро кликнуть оверлей + кнопку ✕ за 200мс | Модалка закрывается один раз | Два `setTimeout` → `onClose()` вызывается дважды | Проверка `if (isClosing) return` в `handleClose` | 🔴 Open |
